#include "main.h"

/**
* print_number -  An entry point
* @n: integer parameter
* Description: 'prints the number
*/
void print_number(int n)
{
int i = 1000000000;

for (; i >= 1; i /= 10)
if (m / i != 0)
{
_putchar((m / i) % 10 + '0');
}
}

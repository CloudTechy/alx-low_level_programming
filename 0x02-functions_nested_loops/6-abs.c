#include "main.h"
/**
* _abs - Entry point
* @r: An input character
* Description: 'the program's description'
* Return: Always 0 (Success)
*/
int _abs(int r)
{
if (r >= 0)
return (r);
else
return (r * -1);
}
